% =========================================================================
%  Fast, efficient, VRAM-friendly LLM serving
%  MiRA Training Course 2026 - 9/4, 10:20-11:00, R546
%
%  Build:   tectonic llm_serving_talk.tex
%  (also compiles with:  lualatex llm_serving_talk.tex  x2 )
%
%  Speaker notes: uncomment ONE of the \setbeameroption lines below.
% =========================================================================
\documentclass[aspectratio=169,10pt]{beamer}

\usetheme{metropolis}
\metroset{numbering=fraction, progressbar=foot, block=fill}

% --- Fonts: XeTeX/LuaTeX get a CJK family for the speaker's name ----------
\usepackage{iftex}
\ifXeTeX
  \usepackage{fontspec}
  \IfFontExistsTF{Noto Sans CJK TC}{%
    \newfontfamily\cjkfamily{Noto Sans CJK TC}%
    \newcommand{\CJK}[2]{{\cjkfamily #1}}%
  }{%
    \IfFontExistsTF{Noto Sans CJK SC}{%
      \newfontfamily\cjkfamily{Noto Sans CJK SC}%
      \newcommand{\CJK}[2]{{\cjkfamily #1}}%
    }{\newcommand{\CJK}[2]{#2}}%
  }
\else\ifLuaTeX
  \usepackage{fontspec}
  \IfFontExistsTF{Noto Sans CJK TC}{%
    \newfontfamily\cjkfamily{Noto Sans CJK TC}%
    \newcommand{\CJK}[2]{{\cjkfamily #1}}%
  }{\newcommand{\CJK}[2]{#2}}
\else
  % pdflatex has no system-font access: fall back to the Latin transcription
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \newcommand{\CJK}[2]{#2}
\fi\fi
% Usage: \CJK{<CJK text>}{<Latin fallback>}

\usepackage{amsmath, amssymb, mathtools}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, fit, backgrounds, calc, decorations.pathreplacing}
\usepackage{url}

\definecolor{accent}{HTML}{1F6FEB}
\definecolor{warn}{HTML}{C2410C}
\definecolor{good}{HTML}{15803D}
\newcommand{\emphc}[1]{\textcolor{accent}{\textbf{#1}}}
\newcommand{\warnc}[1]{\textcolor{warn}{\textbf{#1}}}
\newcommand{\goodc}[1]{\textcolor{good}{\textbf{#1}}}

% --- Badges ---------------------------------------------------------------
\newcommand{\demo}[1]{%
  \tikz[baseline=(x.base)]\node[fill=good, text=white, rounded corners=2pt,
        inner xsep=4pt, inner ysep=1.5pt, font=\bfseries\scriptsize] (x)
        {LIVE DEMO: notebook #1};}
\newcommand{\cutbadge}[1]{%
  \tikz[baseline=(x.base)]\node[fill=warn, text=white, rounded corners=2pt,
        inner xsep=4pt, inner ysep=1.5pt, font=\bfseries\scriptsize] (x)
        {CUT #1};}

% --- LLM mini-diagram: data-flow pipeline, anchored top-right via overlay --
% Shows: Prefill -> KV -> Decode, with Weights feeding Prefill and Decode.
% Each \llmdiagX command places the diagram at the top-right corner using
% remember picture + overlay, so it floats above the slide content.
\tikzset{
  llmbox/.style={draw=black!30, fill=black!4, rounded corners=1pt,
                 minimum height=0.28cm, inner xsep=1.5pt, inner ysep=0.5pt,
                 font=\sffamily\tiny},
  llmhl/.style={draw=accent, fill=accent!18, line width=0.5pt},
  llmar/.style={-{Latex[length=0.8mm]}, black!25},
}
\newcommand{\llmbase}{%
  \node[llmbox] (PF) at (0, 0) {Prefill};
  \node[llmbox] (KV) at (1.05, 0) {KV};
  \node[llmbox] (DC) at (1.95, 0) {Decode};
  \node[llmbox] (WP) at (0, -0.45) {Weights};
  \node[llmbox] (WD) at (1.95, -0.45) {Weights};
  \draw[llmar] (PF)--(KV); \draw[llmar] (KV)--(DC);
  \draw[llmar] (WP)--(PF); \draw[llmar] (WD)--(DC);
}
% Helper: place a tikzpicture at top-right corner of the slide.
% To show the mini-diagrams, swap the two lines below.
\newcommand{\llmoverlay}[1]{}  % hidden
%\newcommand{\llmoverlay}[1]{\begin{tikzpicture}[remember picture, overlay]\node[anchor=north east, inner sep=0pt] at ([xshift=-3mm, yshift=-1mm]current page.north east) {#1};\end{tikzpicture}}
\newcommand{\llmdiagW}{\llmoverlay{\begin{tikzpicture}[scale=0.65]\llmbase
  \node[llmbox, llmhl] at (0,-0.45) {Weights};
  \node[llmbox, llmhl] at (1.95,-0.45) {Weights};\end{tikzpicture}}}
\newcommand{\llmdiagKV}{\llmoverlay{\begin{tikzpicture}[scale=0.65]\llmbase
  \node[llmbox, llmhl] at (1.05,0) {KV};\end{tikzpicture}}}
\newcommand{\llmdiagWKV}{\llmoverlay{\begin{tikzpicture}[scale=0.65]\llmbase
  \node[llmbox, llmhl] at (0,-0.45) {Weights};
  \node[llmbox, llmhl] at (1.95,-0.45) {Weights};
  \node[llmbox, llmhl] at (1.05,0) {KV};\end{tikzpicture}}}
\newcommand{\llmdiagS}{\llmoverlay{\begin{tikzpicture}[scale=0.65]\llmbase
  \node[llmbox, llmhl] at (0,0) {Prefill};
  \node[llmbox, llmhl] at (1.95,0) {Decode};\end{tikzpicture}}}
\newcommand{\llmdiagK}{\llmoverlay{\begin{tikzpicture}[scale=0.65]\llmbase
  \node[llmbox, llmhl] at (0,0) {Prefill};
  \node[llmbox, llmhl] at (1.95,0) {Decode};
  \node[llmbox, llmhl] at (0,-0.45) {Weights};
  \node[llmbox, llmhl] at (1.95,-0.45) {Weights};\end{tikzpicture}}}
\newcommand{\llmdiagSKV}{\llmoverlay{\begin{tikzpicture}[scale=0.65]\llmbase
  \node[llmbox, llmhl] at (0,0) {Prefill};
  \node[llmbox, llmhl] at (1.95,0) {Decode};
  \node[llmbox, llmhl] at (1.05,0) {KV};\end{tikzpicture}}}
\newcommand{\llmdiagAll}{\llmoverlay{\begin{tikzpicture}[scale=0.65]\llmbase
  \node[llmbox, llmhl] at (0,0) {Prefill};
  \node[llmbox, llmhl] at (1.05,0) {KV};
  \node[llmbox, llmhl] at (1.95,0) {Decode};
  \node[llmbox, llmhl] at (0,-0.45) {Weights};
  \node[llmbox, llmhl] at (1.95,-0.45) {Weights};\end{tikzpicture}}}

% --- Speaker notes: uncomment to render -----------------------------------
% \setbeameroption{show notes}
% \setbeameroption{show notes on second screen=right}

\title{Fast, efficient, VRAM-friendly LLM serving}

\author{\CJK{仲翊}{Chung-Yi}}
\institute{MiRA Training Course 2026}
\date{9/4, 10:20--11:00 \quad R546}

\begin{document}


% =========================================================================
\maketitle
\note{\textbf{0:00.} Give your name and the promise: 40 minutes, and then you can size a
deployment on paper. Inference only, no architecture. Say the end time: 11:00.}

% -------------------------------------------------------------------------
\begin{frame}{Deployment problem: one GPU, 70B parameters}
  \vspace{0.5em}
  \emphc{One GPU}. \emphc{70B parameters}.
  \begin{itemize}
    \item Does it fit?
    \item If it fits, how many people can use it at once?
    \item If it is too slow, what do you change \emph{first}?
  \end{itemize}
  \vspace{0.6em}
\end{frame}
\note{\textbf{0:30.} Show of hands: who has downloaded a model and hoped that it
loads? Everyone.}

% -------------------------------------------------------------------------
\begin{frame}{Serving cost is arithmetic on two numbers}
  \begin{alertblock}{}
    \textbf{Memory decides serving cost.}
    Compute the VRAM budget and the memory-bandwidth ceiling. Then each
    deployment decision (quantize, batch, shrink, or buy another GPU)
    follows from arithmetic.
  \end{alertblock}
  \vspace{0.4em}
  \begin{enumerate}
    \item \textbf{Does it fit?} $\rightarrow$ the VRAM budget
    \item \textbf{How fast can it go?} $\rightarrow$ the bandwidth ceiling
    \item \textbf{Which knob do I turn?} $\rightarrow$ a ranked toolbox
  \end{enumerate}
\end{frame}
\note{\textbf{1:30.} Point at the map once. We need one fact about attention, and it
arrives in Movement 1.}

% -------------------------------------------------------------------------
\begin{frame}{Outline}
  \tableofcontents
\end{frame}

% -------------------------------------------------------------------------


% =========================================================================

% =========================================================================

\section{VRAM and bandwidth}

% =========================================================================

\begin{frame}{VRAM usage: weights, KV cache, activations, overhead}
  \vspace{0.2em}
  \llmdiagWKV
  \begin{columns}[c]
    \begin{column}{0.55\textwidth}
      \begin{center}
        \begin{tikzpicture}[
          font=\scriptsize,
          bar/.style={minimum height=0.5cm, inner sep=0pt, anchor=south west,
                      font=\tiny\bfseries},
        ]
        \def\totalW{8.0}  % bar width in cm for 24 GB
        \def\scale{0.333}  % cm per GB

        % --- Bar 1: fp16, 2 users (weights dominate) ---
        \node[font=\tiny, anchor=east] at (-0.1, 0.25) {fp16, 2 users};
        % weights: 14.9 GB
        \node[bar, fill=accent!40, minimum width=14.9*\scale cm, text=black]
          (w1) at (0, 0) {weights 14.9};
        % KV: 2 users * 1 GiB = 2 GB
        \node[bar, fill=warn!40, minimum width=2.0*\scale cm, text=black]
          at (14.9*\scale, 0) {KV 2.0};
        % activations: ~0.1
        \node[bar, fill=good!30, minimum width=0.1*\scale cm]
          at (16.9*\scale, 0) {};
        % overhead: 2 GB
        \node[bar, fill=black!15, minimum width=2.0*\scale cm, text=black]
          at (17.0*\scale, 0) {OH 2};
        % free
        \node[bar, fill=black!5, draw=black!20, minimum width=5.0*\scale cm,
              text=black!50] at (19.0*\scale, 0) {free 5.0};
        % 24 GB marker
        \draw[black!40] (24*\scale, -0.05) -- (24*\scale, 0.55);
        \node[font=\tiny, black!50] at (24*\scale, 0.7) {24\,GB};

        % --- Bar 2: fp16, 7 users (KV fills the card) ---
        \node[font=\tiny, anchor=east] at (-0.1, -0.75) {fp16, 7 users};
        \node[bar, fill=accent!40, minimum width=14.9*\scale cm, text=black]
          at (0, -1.0) {weights 14.9};
        \node[bar, fill=warn!40, minimum width=5.1*\scale cm, text=black]
          at (14.9*\scale, -1.0) {\alert{KV 5.1}};
        \node[bar, fill=good!30, minimum width=0.1*\scale cm]
          at (20.0*\scale, -1.0) {};
        \node[bar, fill=black!15, minimum width=2.0*\scale cm, text=black]
          at (20.1*\scale, -1.0) {OH 2};
        \node[bar, fill=black!5, draw=black!20, minimum width=1.9*\scale cm,
              text=black!50] at (22.1*\scale, -1.0) {\tiny 1.9};
        \draw[black!40] (24*\scale, -1.05) -- (24*\scale, -0.45);

        % --- Bar 3: int4, 7 users (weights shrunk) ---
        \node[font=\tiny, anchor=east] at (-0.1, -1.75) {int4, 7 users};
        \node[bar, fill=accent!25, minimum width=3.7*\scale cm, text=black]
          at (0, -2.0) {w 3.7};
        \node[bar, fill=warn!40, minimum width=5.1*\scale cm, text=black]
          at (3.7*\scale, -2.0) {KV 5.1};
        \node[bar, fill=good!30, minimum width=0.1*\scale cm]
          at (8.8*\scale, -2.0) {};
        \node[bar, fill=black!15, minimum width=2.0*\scale cm, text=black]
          at (8.9*\scale, -2.0) {OH 2};
        \node[bar, fill=black!5, draw=black!20, minimum width=13.1*\scale cm,
              text=black!50] at (10.9*\scale, -2.0) {free 13.1};
        \draw[black!40] (24*\scale, -2.05) -- (24*\scale, -1.45);

        \end{tikzpicture}
      \end{center}
    \end{column}
    \begin{column}{0.42\textwidth}
      \small
      \begin{itemize}\setlength{\itemsep}{0.5em}
        \item \textbf{Weights}: fixed, paid once.
        \item \emphc{KV cache}: per user, per token.
        \item \textbf{Activations}: small at inference.
        \item \textbf{Overhead}: $\approx$ 2\,GB.
      \end{itemize}
      \vspace{0.6em}
      \uncover<2->{\small Llama-3-8B on a 24\,GB card. In fp16,
      weights occupy 14.9\,GB and each 8k-context user adds 1\,GiB of
      KV cache. Quantizing the weights to int4 frees 11.2\,GB for more
      users.}
    \end{column}
  \end{columns}
\end{frame}
\note{\textbf{3:30.} The three bars are the slide. Point at how the KV cache
(red) grows from bar 1 to bar 2 while weights (blue) stay fixed. Then bar 3
shows int4 shrinking the weights and freeing space.

Llama-3-8B: 14.9 GB fp16 weights, 128 KiB per token KV, 8k context = 1 GiB
per user. 7 users fills a 24 GB card in fp16. In int4 the same 7 users leave
13.1 GB free.}

% -------------------------------------------------------------------------
\begin{frame}{Prefill is compute-bound. Decode is bandwidth-bound.}
  \llmdiagK
  \vspace{-0.2em}
  \begin{center}
    \begin{tikzpicture}[
      font=\scriptsize,
      phase/.style={rectangle, rounded corners=2pt, minimum height=0.7cm,
                    inner xsep=4pt, inner ysep=2pt, font=\scriptsize\bfseries},
      ar/.style={-{Latex[length=1.5mm]}, black!40, thick},
      wt/.style={rectangle, draw=accent!50, fill=accent!10, rounded corners=1pt,
                 minimum height=0.3cm, inner xsep=2pt, font=\tiny},
    ]
    % Prefill
    \node[phase, draw=good!60, fill=good!12, minimum width=4cm] (pf)
      at (0, 0) {Prefill};
    \node[font=\tiny, good!70, align=center] at (0, -0.65)
      {all prompt tokens in parallel\\compute-bound: ALUs busy};
    \node[wt] at (0, 0.7) {Weights (read once)};

    % Arrow
    \draw[ar] (2.3, 0) -- (3.5, 0);
    \node[font=\tiny, black!50] at (2.9, 0.2) {KV stored};

    % Decode
    \node[phase, draw=warn!60, fill=warn!12, minimum width=4cm] (dc)
      at (5.8, 0) {Decode};
    \node[font=\tiny, warn!70, align=center] at (5.8, -0.65)
      {one token at a time\\bandwidth-bound: reading weights};
    \node[wt] at (5.8, 0.7) {Weights (read per token)};

    % Token outputs
    \node[font=\tiny, black!40] at (8.5, 0) {$t_1, t_2, t_3, \ldots$};
    \draw[ar] (7.9, 0) -- (8.2, 0);
    \end{tikzpicture}
  \end{center}
  \vspace{0.3em}
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \small \textbf{Prefill} reads the weights once and processes all
      prompt tokens in one batch of large matrix multiplies. The GPU
      arithmetic units are the bottleneck.
    \end{column}
    \begin{column}{0.48\textwidth}
      \small \textbf{Decode} reads the weights once \emph{per output token}.
      The GPU spends most of its time moving bytes from HBM, and the
      arithmetic units are idle.
    \end{column}
  \end{columns}
\end{frame}
\note{\textbf{10:00.} This fact explains the rest of the talk. The sentence to keep
from Movement 2: ``decoding reads all the weights, once per token''.}

% -------------------------------------------------------------------------
\begin{frame}{Decode throughput ceiling: memory bandwidth $\div$ weight bytes}
  \llmdiagW
  \begin{equation*}
    \text{tokens/sec} \;\leq\;
    \frac{\text{memory bandwidth}}{\text{bytes of weights read per token}}
  \end{equation*}
  \vspace{0.6em}
  \begin{itemize}
    \item An \textbf{upper bound}. Better code cannot beat it.
    \item 8B in fp16 reads \textbf{16\,GB} per token.
          In int4 it reads \textbf{4\,GB}.
          Same card, same math, \emphc{four times the ceiling}.
    \item \uncover<2->{Quantization speeds up decoding because it
          \emph{moves fewer bytes}. The arithmetic costs about the same.}
    \item \uncover<3->{Small models get a high ceiling, because the
          ceiling is bandwidth divided by their size.}
  \end{itemize}
\end{frame}
\note{\textbf{11:30.} Kill the ``quantization is faster because fewer FLOPs''
folklore here. int4 arithmetic costs about the same. int4 \emph{traffic}
is 4x cheaper.}

% -------------------------------------------------------------------------
\begin{frame}{Measured efficiency: \texttt{generate()} at 9.4\% of bandwidth ceiling}
  \llmdiagAll
  \hfill\demo{Part 6--7}
  \vspace{0.2em}

  \textbf{RTX 5090, Qwen2.5-0.5B, fp16.} First, does the arithmetic hold?
  \begin{center}\small
    \begin{tabular}{lrr}
      \toprule
      & predicted & measured \\
      \midrule
      weight memory & 0.920\,GB & 0.928\,GB \quad (ratio \emphc{1.008}) \\
      \bottomrule
    \end{tabular}
  \end{center}
  \vspace{0.6em}
  Then, how close to the ceiling does each approach get at batch 1?
  \begin{center}\small
    \begin{tabular}{lrr}
      \toprule
      & tok/s & \textbf{efficiency} \\
      \midrule
      bandwidth ceiling & 1811.6 & 100\% \\
      \midrule
      HuggingFace \texttt{generate()} & \warnc{169.9} & \warnc{9.4\%} \\
      vLLM & \emphc{726} & \emphc{40.1\%} \\
      SGLang & \goodc{701} & \goodc{38.7\%} \\
      \bottomrule
    \end{tabular}
  \end{center}
  \vspace{0.3em}
  {\small RTX 5090, Qwen2.5-0.5B, fp16, batch 1, 128 output tokens.}
\end{frame}
\note{\textbf{12:30.} Run this live. The 1.008 ratio earns their trust in the
arithmetic. The 9.4\% earns you Movement 3. \texttt{generate()} is a
correctness reference.}

% -------------------------------------------------------------------------
\begin{frame}{Report TTFT, ITL and throughput together}
  \begin{center}
    \begin{tabular}{lll}
      \toprule
      \textbf{TTFT} & time to first token & prefill, what the user \emph{feels} \\
      \textbf{ITL / TPOT} & inter-token latency & decode, how fast text appears \\
      \textbf{Throughput} & total tok/s, all users & what you \emph{pay} for \\
      \bottomrule
    \end{tabular}
  \end{center}
  \vspace{1.0em}
  \begin{itemize}
    \item Latency and throughput \emph{trade against each other}.
          Batching improves one and worsens the other.
    \item \warnc{A benchmark without a stated concurrency level tells you nothing.}
    \item ``Our server does 2000 tok/s'': at what batch size, and with
          what TTFT?
  \end{itemize}
\end{frame}
\note{\textbf{15:00.} This slide saves them from a bad paper table. Three
numbers, together, with the concurrency.
End of Movement 2. Target: 18 minutes.}

% =========================================================================

% =========================================================================

\section{Memory management}

% =========================================================================

% --- Weight quantization ---
\begin{frame}{Weight memory: parameters $\times$ bytes per parameter}
  \llmdiagW
  \begin{equation*}
    \text{weight bytes} \;=\; \text{parameters} \times \text{bytes per parameter}
  \end{equation*}
  \begin{center}\small
    fp32 $=4$ \quad fp16 / bf16 $=2$ \quad fp8 / int8 $=1$ \quad int4 $=0.5$
  \end{center}
  \vspace{0.4em}
  \begin{center}
    \begin{tabular}{lrrr}
      \toprule
      model & fp16 & int8 & int4 \\
      \midrule
      8B  &  16\,GB &  8\,GB & 4\,GB \\
      \textbf{70B} & \warnc{140\,GB} & 70\,GB & \emphc{35\,GB} \\
      405B & 810\,GB & 405\,GB & 203\,GB \\
      \bottomrule
    \end{tabular}
  \end{center}
  \vspace{0.5em}
  \warnc{140\,GB} fits on no card you own.
  \emphc{35\,GB} fits on one 40\,GB card, barely.
  \textbf{That row decides the deployment architecture.}
\end{frame}
\note{\textbf{4:30.} Read the 70B row out loud. It is the cheapest useful
calculation in machine learning, and most people skip it before they
download.}

% -------------------------------------------------------------------------
\begin{frame}{\#2--3 Weight and KV cache quantization}
  \llmdiagWKV
  \begin{columns}[T]
    \begin{column}{0.49\textwidth}
      \textbf{\#2 Weights}\\[0.3em]
      \small
      AWQ, GPTQ (post-training, weight-only). FP8 is native on Hopper and
      newer. \emphc{NVFP4} on Blackwell halves FP8 further.\\[0.4em]
      $\approx \frac14$ the VRAM at 4-bit, and several times the decode speed
      because it moves fewer bytes.
    \end{column}
    \begin{column}{0.49\textwidth}
      \textbf{\#3 The KV cache}\\[0.3em]
      \small
      fp8 or int8 cache. This \emph{doubles} the number of users that you can
      hold.\\[0.4em]
      It follows from Movement 1, and few people enable it.
      In our table it was 18 users $\rightarrow$ \emphc{36}.
    \end{column}
  \end{columns}
  \vspace{1.2em}
  \begin{center}
    \begin{minipage}{0.88\textwidth}
      \begin{alertblock}{The rule}
        \centering
        Quality loss is small on most tasks and large on some.\\
        \textbf{Quantize, then re-run your own eval. A published perplexity
        tells you little.}
      \end{alertblock}
    \end{minipage}
  \end{center}
\end{frame}
\note{\textbf{21:30.} Name AWQ, GPTQ and FP8, give the rule, and continue.
If someone asks which one, answer: the one that your engine supports for your
card. The next slide has the details for the curious.}


% -------------------------------------------------------------------------
% --- KV cache ---
\begin{frame}{The KV cache formula}
  \llmdiagKV
  \begin{equation*}
    \underbrace{\text{KV bytes per token}}_{\text{per user, per token}}
    \;=\;
    \underbrace{2}_{\mathclap{\text{K and V}}}
    \;\times\; \text{layers}
    \;\times\; \text{kv\_heads}
    \;\times\; \text{head\_dim}
    \;\times\; \text{bytes per value}
  \end{equation*}
  \vspace{1.2em}
  \begin{itemize}
    \item Each token that you generated stays in memory.
    \item It grows \emph{linearly} with conversation length.
    \item You pay it \emph{once per concurrent user}.
  \end{itemize}
  \vspace{0.6em}
  \emphc{layers, kv\_heads, head\_dim, bytes per value --- all in \texttt{config.json}.}
\end{frame}
\note{\textbf{5:30.} The one fact about attention I assume: decoding needs the keys
and values of every previous token, so you keep them.}

% -------------------------------------------------------------------------
\begin{frame}{KV cache per token: Llama-3-8B worked example}
  \llmdiagKV
  \textbf{Llama-3-8B:} 32 layers, 8 KV heads, head\_dim 128, fp16 cache.
  \vspace{0.6em}
  \begin{align*}
    \text{KV per token}
      &= 2 \times 32 \times 8 \times 128 \times 2\ \text{bytes} \\[0.2em]
    \uncover<2->{&= 131{,}072\ \text{bytes} \;=\; \emphc{128\ \text{KiB per token}}} \\[0.6em]
    \uncover<3->{\text{An 8k conversation} &= 8192 \times 128\ \text{KiB}
      \;=\; \warnc{1\ \text{GiB} \ \text{for one user}}}
  \end{align*}
  \vspace{0.4em}
  \uncover<3->{You pay 16\,GB for the weights once. You pay this 1\,GiB again for
  each person who opens a chat window.}
\end{frame}
\note{\textbf{6:30.} Do the multiplication aloud, slowly. The whole
talk hangs on this slide. Pause after ``for one user''.}

% -------------------------------------------------------------------------
\begin{frame}{Grouped-Query Attention reduces KV cache by $4\times$}
  \llmdiagKV
  \begin{center}
    \begin{tabular}{lrrr}
      \toprule
      model & KV heads & KV per token & 8k conversation \\
      \midrule
      Llama-2-7B \small(MHA) & 32 & \warnc{512\,KiB} & 4\,GiB \\
      Llama-3-8B \small(GQA) &  8 & \emphc{128\,KiB} & 1\,GiB \\
      \bottomrule
    \end{tabular}
  \end{center}
  \vspace{1.0em}
  \begin{center}\large
    The \emph{smaller} model costs \warnc{four times more cache per token.}
  \end{center}
  \vspace{0.8em}
  Only one thing changed. \emphc{Grouped-Query Attention} shares one set of
  K/V across several query heads, so \texttt{kv\_heads} decreases from 32 to 8.
  Same formula, one quarter of the memory.

  \vspace{0.2em}
  \begin{center}
    \begin{tikzpicture}[font=\tiny,
      hd/.style={circle, minimum size=4mm, inner sep=0pt, draw=black!50,
                 font=\tiny\bfseries},
      qh/.style={hd, fill=accent!20, draw=accent!60},
      kh/.style={hd, fill=good!25, draw=good!60},
      ar/.style={-{Latex[length=1mm]}, black!40}]
    \node[font=\scriptsize\bfseries] at (0,1.4) {MHA};
    \foreach \i in {0,...,3} {
      \node[qh] (mq\i) at (\i*0.55-0.825, 0.8) {Q};
      \node[kh] (mk\i) at (\i*0.55-0.825, 0.15) {KV};
      \draw[ar] (mq\i)--(mk\i); }
    \node[font=\scriptsize\bfseries] at (3.2,1.4) {GQA};
    \foreach \i in {0,...,3}
      \node[qh] (gq\i) at (\i*0.55+2.375, 0.8) {Q};
    \node[kh] (gk0) at (2.65, 0.15) {KV};
    \node[kh] (gk1) at (3.75, 0.15) {KV};
    \draw[ar] (gq0)--(gk0); \draw[ar] (gq1)--(gk0);
    \draw[ar] (gq2)--(gk1); \draw[ar] (gq3)--(gk1);
    \node[font=\scriptsize\bfseries] at (6.4,1.4) {MQA};
    \foreach \i in {0,...,3}
      \node[qh] (aq\i) at (\i*0.55+5.575, 0.8) {Q};
    \node[kh] (ak0) at (6.4, 0.15) {KV};
    \foreach \i in {0,...,3} \draw[ar] (aq\i)--(ak0);
    \end{tikzpicture}
  \end{center}
\end{frame}
\note{\textbf{7:30.} Parameter count is a bad proxy for servability. If
somebody asks ``is a 7B easier to serve than an 8B'', this is the answer:
read \texttt{num\_key\_value\_heads}, not the name.

Ainslie et al.\ uptrained T5-XXL from MHA to GQA-8 in 5\% of the original
training steps. Inference was 5.4$\times$ faster (1.51\,s $\rightarrow$
0.28\,s) with no measurable quality loss. arXiv:2305.13245.}


% -------------------------------------------------------------------------
\begin{frame}{PagedAttention: the KV cache as virtual memory}
  \llmdiagKV
\vspace{-0.2em}
\begin{center}
\begin{tikzpicture}[
  font=\scriptsize,
  blk/.style={draw=black!55, minimum width=0.44cm, minimum height=0.44cm,
              inner sep=0pt, font=\tiny},
  useA/.style={blk, fill=accent!35},
  useB/.style={blk, fill=good!35},
  waste/.style={blk, fill=black!8, dashed, draw=black!35},
  ttl/.style={font=\small\bfseries},
  ar/.style={-{Latex[length=1.6mm]}}
]

% ================= LEFT: contiguous allocation =================
\node[ttl, warn] at (2.3, 4.85) {Contiguous: reserve for the worst case};

\node[anchor=east, font=\tiny] at (0.30, 3.90) {req A};
\foreach \i in {0,1,2}   {\node[useA]  at (0.60+\i*0.50, 3.90) {};}
\foreach \i in {3,...,7} {\node[waste] at (0.60+\i*0.50, 3.90) {};}

\node[anchor=east, font=\tiny] at (0.30, 3.25) {req B};
\foreach \i in {0,...,4} {\node[useB]  at (0.60+\i*0.50, 3.25) {};}
\foreach \i in {5,6,7}   {\node[waste] at (0.60+\i*0.50, 3.25) {};}

\node[font=\tiny, black!60] at (2.30, 2.72)
  {grey $=$ allocated, never used};

\node[text width=4.7cm, align=center, font=\tiny, warn] at (2.30, 1.90)
  {\textbf{internal fragmentation $+$ over-reservation}\\[0.15em]
   prior systems used only \textbf{20.4--38.2\%}\\
   of the KV memory they allocated};

% ================= divider =================
\draw[black!25, dashed] (5.55, 1.10) -- (5.55, 5.10);

% ================= RIGHT: paged allocation =================
\node[ttl, accent] at (9.10, 4.85) {Paged: fixed-size blocks, on demand};

% --- logical blocks of request A ---
\node[anchor=west, font=\tiny, black!65] at (6.30, 4.32) {req A, logical blocks};
\node[useA] (LA0) at (6.50, 3.85) {0};
\node[useA] (LA1) at (7.00, 3.85) {1};
\node[useA] (LA2) at (7.50, 3.85) {2};

% --- block table ---
\node[draw=black!55, fill=black!4, rounded corners=2pt, align=left,
      font=\tiny, inner sep=4pt, anchor=north] (BT) at (11.45, 4.25)
  {\textbf{block table}\\ $0 \rightarrow$ P3\\ $1 \rightarrow$ P0\\ $2 \rightarrow$ P6};

\draw[ar, black!60] (LA2.east) -- (BT.west);

% --- physical block pool ---
\node[anchor=west, font=\tiny, black!65, align=left] at (10.35, 1.90)
  {physical\\ block pool};
\node[useA] (P0) at (6.50, 1.90) {P0};
\node[useB] (P1) at (7.00, 1.90) {P1};
\node[useB] (P2) at (7.50, 1.90) {P2};
\node[useA] (P3) at (8.00, 1.90) {P3};
\node[useB] (P4) at (8.50, 1.90) {P4};
\node[blk]  (P5) at (9.00, 1.90) {};
\node[useA] (P6) at (9.50, 1.90) {P6};
\node[useB] (P7) at (10.00, 1.90) {P7};

\draw[ar, accent] (BT.south) .. controls (11.0, 2.95) and (7.0, 3.05) .. (P0.north);
\draw[ar, accent] (BT.south) .. controls (10.9, 2.80) and (8.4, 2.90) .. (P3.north);
\draw[ar, accent] (BT.south) .. controls (10.8, 2.55) and (10.0, 2.60) .. (P6.north);

\node[text width=5.4cm, align=center, font=\tiny, accent] at (8.60, 1.15)
  {blocks need not be contiguous $\cdot$ waste is bounded by \emph{one} block\\[0.15em]
   identical prefixes are \textbf{shared}, copy-on-write};

\end{tikzpicture}
\end{center}
\vspace{-0.2em}
{\scriptsize Kwon et al., \emph{Efficient Memory Management for LLM Serving with
PagedAttention}, arXiv:2309.06180.}
\end{frame}
\note{\textbf{20:00.} This is the most important picture in the talk. Say the OS
analogy: these are page tables. Then the payoff: waste decreases
from ``the whole unused tail'' to ``at most one block''. Two requests
with the same system prompt point at the same physical blocks.}

% -------------------------------------------------------------------------
\begin{frame}{SGLang: RadixAttention for longest-prefix matching}
  \llmdiagKV
  \vspace{-0.6em}
  \begin{columns}[c]
    \begin{column}{0.48\textwidth}
      \centering
      \begin{tikzpicture}[
        font=\tiny,
        nd/.style={circle, draw=black!40, fill=black!5, minimum size=3.5mm,
                   inner sep=0pt},
        hit/.style={nd, draw=accent, fill=accent!20},
        new/.style={nd, draw=good, fill=good!20},
        evict/.style={nd, draw=warn, fill=warn!15, dashed},
        edg/.style={-{Latex[length=1mm]}, black!40, thick},
        lab/.style={font=\tiny, midway, above, black!60},
      ]
      \node[nd] (root) at (0, 0) {};
      \node[font=\tiny, black!50, left=1mm of root] {root};
      % Branch 1: system prompt (cache hit)
      \node[hit] (a1) at (1.5, 0.8) {};
      \node[hit] (a2) at (3.0, 1.2) {};
      \node[new] (a3) at (4.5, 1.2) {};
      \draw[edg, accent] (root)--(a1) node[lab] {sys prompt};
      \draw[edg, accent] (a1)--(a2) node[lab] {doc A};
      \draw[edg, good] (a2)--(a3) node[lab, good!60] {new query};
      % Branch 2: same system prompt, different doc
      \node[hit] (b1) at (3.0, 0.0) {};
      \draw[edg, accent] (a1)--(b1) node[lab, below] {doc B};
      % Branch 3: evicted
      \node[evict] (c1) at (1.5, -0.8) {};
      \draw[edg, warn, dashed] (root)--(c1) node[lab, below] {old prefix};
      % Legend
      \node[font=\tiny, align=left, black!55] at (2.3, -1.6)
        {\textcolor{accent}{$\bullet$} cache hit \quad
         \textcolor{good}{$\bullet$} new \quad
         \textcolor{warn}{$\bullet$} evicted (LRU)};
      \end{tikzpicture}
    \end{column}
    \begin{column}{0.48\textwidth}
      \small
      \begin{itemize}\setlength{\itemsep}{0.4em}
        \item Each \textbf{edge} stores a token span and its KV cache pages.
              Each \textbf{node} is a branch point where cached prompts diverge.
        \item A new request walks from the root and finds the
              \alert{longest shared prefix}, reusing its KV pages.
        \item vLLM hashes fixed-size blocks: exact match or nothing.
              A radix tree matches \alert{partial overlaps of any length}.
        \item Eviction removes the \textbf{least-recently-used leaf}.
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}
\note{\textbf{25:00.} The diagram is from the SGLang blog (lmsys.org, Jan 2024,
Figure 4). Blue = cache hit (reused KV pages), green = new tokens, dashed
orange = evicted. Point at the branch point after ``sys prompt'': two
different documents share the same system-prompt KV pages. A hash-based cache
misses this if the documents start at different block boundaries.}


% -------------------------------------------------------------------------
\begin{frame}{\#4 Prefix caching: reuse KV blocks across requests}
  \llmdiagKV
  \begin{itemize}
    \item Each request begins with the same 2000-token system prompt.
    \item Each RAG request begins with the same retrieved documents.
    \item \emphc{Prefill it once. Reuse the KV blocks for each later request.}
  \end{itemize}
  \vspace{1.0em}
  \begin{center}
    \begin{minipage}{0.88\textwidth}
      \begin{block}{Why it is nearly free}
        Those KV blocks already exist in memory. PagedAttention already
        lets two requests point at the same physical block. PagedAttention makes prefix
        caching possible, because two requests can share the same physical blocks.
      \end{block}
    \end{minipage}
  \end{center}
  \vspace{0.8em}
  It costs a flag and changes no output. In a RAG deployment it is often
  the largest latency win.
\end{frame}
\note{\textbf{22:30.} Example: an agent with a long tool-definition preamble
pays for that preamble on each call, unless you cache it. It is the
cheapest item in the toolbox.}

% =========================================================================
% --- Batching ---
\begin{frame}{Concurrent user capacity: VRAM minus weights, divided by KV per user}
  \vspace{1.2em}
  \llmdiagWKV
  \begin{center}
    \begin{minipage}{0.86\textwidth}
      \begin{alertblock}{The consequence}
        You divide the VRAM that remains \emph{after} the weights among the
        concurrent users.

        \vspace{0.4em}
        The \textbf{KV cache} sets your \textbf{throughput}.
      \end{alertblock}
    \end{minipage}
  \end{center}
  \vspace{1.0em}
  \begin{center}\large
    $\text{users} \;\approx\;
      \dfrac{\text{VRAM} - \text{weights} - \text{overhead}}
            {\text{KV per token} \times \text{context length}}$
  \end{center}
\end{frame}
\note{\textbf{8:30.} Say it twice. They must still have this sentence in a year.
Each tool in the toolbox shrinks the weights term or the denominator.}


% =========================================================================
\begin{frame}{Batching: $15\times$ throughput at $4\%$ latency cost per user}
  \llmdiagS
  \hfill\demo{Part 8}
  \vspace{0.2em}

  The GPU reads the weights \emph{once for the whole batch}. So a bigger batch is
  almost pure profit.
  \begin{center}
    \begin{tabular}{rrrr}
      \toprule
      batch & total tok/s & per-user tok/s & wall time \\
      \midrule
       1 &  169.3 & 169.3 & 0.39\,s \\
      16 & \emphc{2594.6} & 162.2 & 0.39\,s \\
      \bottomrule
    \end{tabular}
  \end{center}
  \vspace{0.5em}
  \begin{center}\small RTX 5090, Qwen2.5-0.5B, 64 new tokens.\end{center}
  \vspace{0.4em}
  \begin{itemize}
    \item \textbf{Same wall-clock time.} You serve sixteen users instead of one.
    \item \emphc{The KV cache capacity} limits the batch size.
  \end{itemize}
\end{frame}
\note{\textbf{13:30.} Run this live too. It is the most persuasive number in the talk.
Pause on the 0.39\,s column: the sixteenth user cost you nothing. Cache
capacity is throughput.}

% -------------------------------------------------------------------------

% =========================================================================

\section{Compute acceleration}

% =========================================================================

\begin{frame}{Kernel fusion: N HBM round-trips reduced to one}
  \llmdiagK
  \vspace{-0.4em}
  \begin{center}
    \begin{tikzpicture}[
      font=\tiny,
      op/.style={rectangle, draw=black!40, fill=black!5, rounded corners=1pt,
                 minimum width=1.0cm, minimum height=0.35cm, inner sep=1pt},
      hbm/.style={rectangle, draw=warn!60, fill=warn!10, rounded corners=1pt,
                  minimum width=5.5cm, minimum height=0.35cm, inner sep=1pt,
                  font=\tiny\bfseries},
      ar/.style={-{Latex[length=0.8mm]}, black!30},
    ]
    % Unfused: N round-trips
    \node[font=\tiny\bfseries, black!60] at (-1.8, 1.0) {unfused};
    \node[hbm] (hbm1) at (1.5, 1.5) {HBM};
    \node[op] (r1) at (-0.3, 1.0) {RMSNorm};
    \node[op] (r2) at (1.0, 1.0) {RoPE};
    \node[op] (r3) at (2.3, 1.0) {SwiGLU};
    \node[op] (r4) at (3.5, 1.0) {Quant};
    \foreach \n in {r1,r2,r3,r4} {
      \draw[ar, warn] (\n.north) -- ++(0,0.25);
      \draw[ar, warn] (\n.north) ++(0.15,0.25) -- ++(0,-0.25);
    }
    \node[font=\tiny, warn, align=center] at (5.2, 1.25) {$4\times$\\read+write};

    % Fused: 1 round-trip
    \node[font=\tiny\bfseries, black!60] at (-1.8, -0.1) {fused};
    \node[hbm] (hbm2) at (1.5, 0.4) {HBM};
    \node[op, draw=good, fill=good!10, minimum width=4.6cm] (fused)
      at (1.5, -0.1) {RMSNorm + RoPE + SwiGLU + Quant};
    \draw[ar, good] (fused.north) -- ++(0,0.25);
    \draw[ar, good] (fused.north) ++(0.15,0.25) -- ++(0,-0.25);
    \node[font=\tiny, good!60, align=center] at (5.2, 0.15) {$1\times$\\read+write};
    \end{tikzpicture}
  \end{center}
  \vspace{0.2em}
  \scriptsize
  \begin{center}
    \begin{tabular}{@{}llp{6.5cm}@{}}
      \toprule
      & \textbf{approach} & \textbf{how it fuses} \\
      \midrule
      \textbf{FlashAttention} & hand-written CUDA
        & fuses Q$\cdot$K$^T$ $\rightarrow$ softmax $\rightarrow$ $\times$V
          in SRAM tiles, avoids materializing the $O(N^2)$ attention matrix \\
      \textbf{Triton} & Python DSL
        & tile-level GPU code, compiler handles memory staging \\
      \textbf{torch.compile} & automatic
        & traces PyTorch, applies fusion passes, emits Triton kernels \\
      \textbf{JAX XLA} & automatic
        & HLO fusion passes over the computation graph \\
      \textbf{TensorRT} & offline
        & graph-level layer fusion + per-GPU GEMM auto-tuning \\
      \textbf{FlashRT} & hand-written + CUDA graph
        & fused memory-bound ops, delegates GEMM to cuBLAS \\
      \bottomrule
    \end{tabular}
  \end{center}
\end{frame}
\note{The single principle: a chain of N unfused kernels does N HBM
round-trips. A fused kernel reads HBM once and writes once, cutting memory
traffic by up to N$\times$. The spectrum runs from fully manual (FlashAttention,
FlashRT) to fully automatic (torch.compile, XLA), with Triton and TensorRT in
between. Manual fusion achieves the highest efficiency but requires GPU
programming expertise.

Liger-Kernel applies the same technique to training (fused RMSNorm + SwiGLU),
claiming +20\% throughput and $-$60\% memory.}

% -------------------------------------------------------------------------
\begin{frame}{\#5 Speculative decoding: draft, verify, accept}
  \llmdiagK
  \vspace{-0.4em}
  \begin{center}
    \begin{tikzpicture}[
      font=\scriptsize,
      tok/.style={rectangle, draw=black!40, fill=black!5, rounded corners=1pt,
                  minimum width=0.7cm, minimum height=0.45cm, inner sep=1pt},
      acc/.style={tok, draw=good, fill=good!15},
      rej/.style={tok, draw=warn, fill=warn!15},
      draft/.style={tok, draw=accent!50, fill=accent!8},
      ar/.style={-{Latex[length=1.2mm]}, black!35},
    ]
    % Draft phase
    \node[font=\tiny\bfseries, black!60] at (-1.2, 0.8) {draft model};
    \foreach \i/\t in {0/The, 1/cat, 2/sat, 3/on, 4/a}
      \node[draft] (d\i) at (\i*1.0, 0.8) {\t};
    \foreach \i in {0,...,3}
      \pgfmathtruncatemacro{\j}{\i+1}
      \draw[ar, accent!50] (d\i)--(d\j);

    % Verify phase
    \node[font=\tiny\bfseries, black!60] at (-1.2, 0) {big model};
    \node[font=\tiny, black!40] at (-1.2, -0.35) {(1 forward pass)};
    \node[acc] (v0) at (0, 0) {The};
    \node[acc] (v1) at (1.0, 0) {cat};
    \node[acc] (v2) at (2.0, 0) {sat};
    \node[rej] (v3) at (3.0, 0) {\color{warn}on};
    \node[font=\tiny, warn] at (3.0, -0.45) {reject};
    \node[acc] (v3c) at (4.0, 0) {upon};
    \node[font=\tiny, good!60] at (4.0, -0.45) {correct};

    % Arrows down from draft to verify
    \foreach \i in {0,...,3}
      \draw[ar] (d\i)--(v\i);
    \end{tikzpicture}
  \end{center}
  \vspace{0.2em}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \small
      \textbf{Why it works:} decode is bandwidth-bound, so the GPU has spare
      compute. Verifying $k$ draft tokens reads the weights \textbf{once}
      (same bandwidth as 1 token) but uses $k\times$ the arithmetic.
    \end{column}
    \begin{column}{0.44\textwidth}
      \small
      \textbf{Why n-gram drafts work:} the acceptance rate only needs to
      exceed $1/k$ to break even. Natural language has enough local
      repetition (articles, prepositions, closing brackets) that even a
      simple n-gram model achieves 50--70\% acceptance.
    \end{column}
  \end{columns}
  \vspace{0.3em}
  \begin{center}\small
    Modified rejection sampling preserves the \emphc{exact target distribution}.
  \end{center}
\end{frame}
\note{The diagram shows 5 draft tokens verified in one forward pass. Three
accepted (green), one rejected (orange ``on'' replaced by ``upon''), and the
correction token sampled from the residual distribution. The key insight: the
big model reads its weights once for all 5 tokens, so the bandwidth cost is
1 token while producing up to 5. An n-gram draft model works because even
50\% acceptance at $k=5$ gives 2.5 tokens per weight read.

The identical-distribution guarantee comes from modified rejection sampling:
accept if $p_\text{target} \geq p_\text{draft}$, otherwise accept with
probability $p_\text{target}/p_\text{draft}$ and sample a correction from
the residual. Leviathan et al., arXiv:2302.01318.}

% -------------------------------------------------------------------------
\begin{frame}{Multi-Token Prediction: a built-in draft for speculative decoding}
  \textbf{MTP} trains a model to predict $D$ tokens ahead. At inference
  the extra heads serve as a \emph{built-in draft} for speculative decoding,
  so no separate draft model is needed.
  \vspace{0.6em}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \textbf{Training}\\[0.3em]
      \small
      $D$ auxiliary heads share the backbone and the embedding layer. Each head
      predicts the token $d$ positions ahead. The MTP loss is averaged and
      weighted by $\lambda$ (DeepSeek-V3 uses $\lambda{=}0.3$ for the first 10T
      tokens, then $0.1$).
    \end{column}
    \begin{column}{0.44\textwidth}
      \textbf{Inference}\\[0.3em]
      \small
      The auxiliary heads propose tokens, and the main model verifies them in
      one forward pass (speculative decoding). The heads can also be
      discarded, and the main model runs independently.
    \end{column}
  \end{columns}
  \vspace{0.8em}
  \begin{center}\small
    DeepSeek-V3 ablation ($D{=}1$): HumanEval improved from 20.7\% to 26.8\%
    (+6.1) on the small MoE, and from 44.5\% to 53.7\% (+9.2) on the large
    MoE. The auxiliary loss regularizes training, so quality improves even
    when the extra heads are discarded at inference.
  \end{center}
\end{frame}
\note{The training effect is larger than the inference effect: the auxiliary
heads regularize and improve quality even when discarded. The inference
bonus is speculative decoding with no separate draft model.

DeepSeek-V3 technical report (arXiv:2412.19437). NVIDIA NeMo supports MTP via
\texttt{mtp\_num\_layers} and \texttt{mtp\_loss\_scaling\_factor}.}

\begin{frame}{SGLang CUDA graph cliff: a default that halves throughput}
  Same SGLang, same machine, one config value changed.
  \vspace{0.4em}
  \begin{center}
    \begin{tabular}{rrr}
      \toprule
      \textbf{batch} & \textbf{stock config} & \textbf{graphs raised to 64} \\
      \midrule
      16 & 9,680 & 9,625 \\
      32 & \warnc{5,657} & \goodc{18,491} \\
      64 & \warnc{11,152} & \goodc{32,523} \\
      \bottomrule
    \end{tabular}
  \end{center}
  \vspace{0.6em}
  SGLang captures decode CUDA graphs for
  \texttt{bs = 1 2 4 8 12 16 24} and stops. Past 24 there is no graph, so it
  uses eager mode. \texttt{cuda\_graph\_max\_bs\_decode} corrects it.
  \vspace{0.5em}
  \begin{center}
    \begin{minipage}{0.93\textwidth}
      \begin{alertblock}{The lesson}
        The missing CUDA toolkit showed a stack trace. This one
        produced a \textbf{plausible and wrong} throughput curve.
        \emphc{Benchmark at the concurrency that you will serve.}
      \end{alertblock}
    \end{minipage}
  \end{center}
\end{frame}
\note{\textbf{30:30.} If you measure only to 16, the
default looks correct. If you measure only at 32 and 64, SGLang looks like the
slow engine. Neither is true. A wrong number is more dangerous than a crash, because
a crash stops you.}

% -------------------------------------------------------------------------

% =========================================================================

\section{Engines and measurements}

% =========================================================================

\begin{frame}{\#1 Serving engine: continuous batching and PagedAttention}
  \llmdiagS
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \textbf{Continuous batching}\\[0.3em]
      \small A finished request \emph{leaves the batch immediately}. It does
      not wait for the slowest one in its group. New requests join on the
      next step. There is no head-of-line blocking.
    \end{column}
    \begin{column}{0.48\textwidth}
      \textbf{PagedAttention}\\[0.3em]
      \small The KV cache in fixed-size \emph{pages}, like OS virtual memory.
      Blocks need not be contiguous, and you stop reserving for the worst
      case.
    \end{column}
  \end{columns}
  \vspace{1.2em}
  \begin{center}
    \begin{minipage}{0.88\textwidth}
      \begin{alertblock}{}
        \centering
        To replace \texttt{model.generate()} in a for-loop with
        \texttt{vllm serve} is \emph{a one-line change}. It is the
        largest single win available to you.
      \end{alertblock}
    \end{minipage}
  \end{center}
\end{frame}
\note{\textbf{19:00.} They will want to write their own batching loop.
Tell them plainly: you will get it wrong, and the people who got it correct
gave it away.}

% -------------------------------------------------------------------------
\begin{frame}{vLLM: PagedAttention and continuous batching}
  \llmdiagSKV
  \begin{center}
    \begin{minipage}{0.9\textwidth}
      \begin{block}{Reported in the paper (arXiv:2309.06180)}
        \centering
        \textbf{2--4$\times$ throughput} $\cdot$
        \textbf{1.7--2.7$\times$} the request rate of Orca $\cdot$
        up to \textbf{22$\times$} FasterTransformer
      \end{block}
    \end{minipage}
  \end{center}
  \vspace{0.6em}
  \textbf{The rest of the box}, all on by default or one flag away:
  \begin{itemize}\small
    \item \textbf{Continuous batching}: the V1 scheduler gives each request
          a token budget and treats prompt and output tokens alike.
    \item \textbf{Hash-based prefix caching}, \textbf{chunked prefill}
          (on by default, decode prioritized), and \textbf{CUDA graphs}.
    \item \textbf{Speculative decoding}: n-gram, suffix, EAGLE, MTP, draft model.
    \item \textbf{Quantization}: FP8 / INT8 / INT4 / GPTQ / AWQ / NVFP4,
          plus an \textbf{FP8 KV cache}.
  \end{itemize}
\end{frame}
\note{\textbf{24:00.} They will remember one idea per project.
vLLM = PagedAttention. Everything else on this slide is a bullet that they can
find later. Do not read the list.}

% -------------------------------------------------------------------------
\begin{frame}{Engine selection: vLLM vs.\ SGLang}
  \vspace{0.5em}
  \begin{center}
    \begin{tabular}{ll}
      \toprule
      \textbf{vLLM} & the default. Start here. \\[0.5em]
      \textbf{SGLang} & you live on \emph{shared prefixes} or \emph{structured
                        output} \\
                      & (agents, tool calling, JSON schemas). \\[0.5em]
      \bottomrule
    \end{tabular}
  \end{center}
  \vspace{1.2em}
  \begin{center}\large
  \end{center}
\end{frame}
\note{\textbf{28:00.} Be decisive. They came for a recommendation. End of Movement 3b.}

% =========================================================================

% =========================================================================
\begin{frame}{Measured throughput: HF, vLLM, SGLang on RTX 5090}
  \small
  Same RTX 5090, same Qwen2.5-0.5B in fp16, same 128 output tokens.
  Total tok/s across all concurrent requests.
  \vspace{0.4em}
  \begin{center}
    \begin{tabular}{rrrr}
      \toprule
      \textbf{batch} & \textbf{HF \texttt{generate()}} & \textbf{vLLM} &
      \textbf{SGLang} \\
      \midrule
      1  & 165   & \textbf{726}    & 701 \\
      8  & 1,275 & \textbf{5,155}  & 4,977 \\
      16 & 2,548 & \textbf{9,880}  & 9,625 \\
      64 & 9,782 & \textbf{37,265} & 32,523 \\
      \bottomrule
    \end{tabular}
  \end{center}
  \vspace{0.5em}
  \begin{itemize}\small
    \item \emphc{Batching does not explain it.} \texttt{generate()} batches correctly,
          and all four scale almost linearly. The engines are
          $\sim$4$\times$ faster \emph{per token}.
    \item At batch 1, \texttt{generate()} reaches \textbf{9\%} of this model's
          bandwidth ceiling. vLLM reaches \textbf{40\%}.
    \item \textbf{On a 0.5\,B model, dispatch overhead dominates.}
  \end{itemize}
\end{frame}
\note{\textbf{29:00.} These are our own numbers, from two days ago. The 4$\times$ is the
headline. Stop any "which is best" question fast: on a model this small you
measure per-request overhead.}

% -------------------------------------------------------------------------

% =========================================================================

\section{Robot policy serving}

% =========================================================================

\begin{frame}{Robot policy serving: batch 1, 20\,ms deadline}
  \vspace{0.3em}
  \begin{center}
    \begin{tabular}{lll}
      \toprule
       & \textbf{LLM server} & \textbf{Robot policy} \\
      \midrule
      wants        & throughput & a \emph{deadline} \\
      batch size   & as large as memory allows & \warnc{1, always} \\
      latency target & hundreds of ms, on average & \warnc{20\,ms, every time} \\
      users        & many, independent & one stream \\
      failure mode & a slow reply & the robot misses its control step \\
      \bottomrule
    \end{tabular}
  \end{center}
  \vspace{1.0em}
  \begin{center}
    \begin{minipage}{0.9\textwidth}
      \begin{alertblock}{}
        \centering
        Most tricks in the last section optimize the first column.\\
        \textbf{Continuous batching does nothing when the batch is 1.}
      \end{alertblock}
    \end{minipage}
  \end{center}
\end{frame}
\note{\textbf{28:30.} This slide makes the talk land in this room. 50\,Hz means
20\,ms at the tail, every step. A p99 of
60\,ms is a robot that jerks.}

% -------------------------------------------------------------------------
\begin{frame}{vLLM-Omni: a pipeline of stages, and one stage emits actions}
  A superset of vLLM for omni-modality: text, image, audio, video and
  \emphc{actions}.
  \vspace{0.6em}
  \begin{itemize}
    \item \textbf{Stage-based architecture.} A \emph{stage} is a model
          component with its own execution loop, scheduling, memory and
          parallelism. An autoregressive stage and a diffusion/DiT stage sit
          in one pipeline, with overlapped execution and disaggregated
          resources.
    \item \textbf{\texttt{Pi0Pipeline}} takes raw robot observations, does
          flow-matching denoising, and returns continuous \emph{action chunks}.
    \item You serve it with \texttt{vllm serve --deploy-config pi0.yaml}. It
          exposes an \textbf{OpenPI realtime robot endpoint}
          (\texttt{/v1/realtime/robot/openpi}).
    \item Also GR00T-N1.7, InternVLA-A1, Cosmos3 action policy.
  \end{itemize}
\end{frame}
\note{\textbf{29:30.} The idea worth keeping: a VLA is a pipeline of components
with different execution shapes, so the server schedules them separately.}

% -------------------------------------------------------------------------
\begin{frame}{FlashRT: no compile, hand-written kernels, one CUDA graph}
  \llmdiagK
  \emphc{FlashRT runs without a compile step or an ONNX export}.
  \vspace{0.6em}
  \begin{itemize}
    \item \textbf{Hand-written CUDA kernels} for the memory-bound operations:
          norm, activation, RoPE, quantization.
    \item \textbf{Delegates the compute-bound ones} to cuBLASLt / CUTLASS /
          FlashAttention-2.
    \item \textbf{The entire forward pass is one static CUDA graph},
          so replay skips Python dispatch.
    \item \textbf{Static FP8 (E4M3) and NVFP4}, with calibration scales cached
          on disk. The $(1+w)$ of RMSNorm folds into the next GEMM at load time.
  \end{itemize}
  \vspace{0.5em}
  \begin{center}\small
    Batch size 1 means dispatch overhead \emph{is} your latency budget.
  \end{center}

  \vspace{0.3em}
  \begin{center}\scriptsize
    \begin{tabular}{llrl}
      \toprule
      model & hardware & latency & speedup \\
      \midrule
      pi0.5, FP8 & RTX 5090 & \textbf{17.6\,ms} (57\,Hz) & \\
      pi0.5, FP8 & Jetson Thor & \textbf{44.0\,ms} (23\,Hz)
        & \emphc{13.9$\times$} vs 714\,ms \\
      \bottomrule
    \end{tabular}
    \quad {\tiny\warnc{FlashRT reported. Not reproduced.}}
  \end{center}
\end{frame}
\note{\textbf{30:30.} At batch 1 with a 20\,ms budget, Python dispatch of a
few hundred kernels takes a large fraction of the frame. One
CUDA graph replaces all of it with a single launch.}


% =========================================================================

% =========================================================================

\section{Deployment decisions}

% =========================================================================

\begin{frame}{Deployment decision tree: fit and speed}
\vspace{0.9em}
\begin{center}
\begin{tikzpicture}[
  font=\scriptsize,
  box/.style={draw=black!55, rounded corners=2pt, fill=black!3,
              minimum height=0.85cm, inner xsep=5pt, align=center,
              font=\footnotesize},
  act/.style={box, fill=accent!12, draw=accent!70},
  q/.style={box, fill=black!6, font=\footnotesize\bfseries},
  ar/.style={-{Latex[length=1.7mm]}, black!60}
]
% --- row 1: does it fit? ---
\node[q]   (fit)  at (0,3.0)   {Does it fit?};
\node[act] (q4)   at (3.0,3.0) {quantize weights\\ int4};
\node[act] (kv)   at (6.0,3.0) {quantize KV\\ fp8};
\node[act] (sm)   at (9.0,3.0) {smaller model};
\node[act] (gpu)  at (11.9,3.0) {more GPUs\\ \emph{last}};

\draw[ar] (fit) -- node[above, font=\tiny, warn] {no} (q4);
\draw[ar] (q4)  -- node[above, font=\tiny, warn] {still no} (kv);
\draw[ar] (kv)  -- node[above, font=\tiny, warn] {still no} (sm);
\draw[ar] (sm)  -- node[above, font=\tiny, warn] {still no} (gpu);

% --- row 2: fast enough? ---
\node[q]   (fast) at (0,1.0)   {Fast enough?};
\node[act] (bat)  at (3.0,1.0) {batch harder};
\node[act] (pre)  at (6.0,1.0) {prefix cache};
\node[act] (spec) at (9.0,1.0) {speculative\\ decoding};
\node[act] (gpu2) at (11.9,1.0) {more GPUs\\ \emph{last}};

\draw[ar] (fast) -- node[above, font=\tiny, warn] {no} (bat);
\draw[ar] (bat)  -- node[above, font=\tiny, warn] {still no} (pre);
\draw[ar] (pre)  -- node[above, font=\tiny, warn] {still no} (spec);
\draw[ar] (spec) -- node[above, font=\tiny, warn] {still no} (gpu2);

\draw[ar, good, thick] (fit.south) -- node[left, font=\tiny, good] {yes} (fast.north);

\node[align=center, font=\scriptsize, black!65] at (6.0,-0.25)
  {Each arrow is cheaper than the arrow to its right.\\
   Buy a GPU after you run out of arithmetic.};
\end{tikzpicture}
\end{center}
\end{frame}
\note{\textbf{32:00.} Let them photograph it. Money is the last resort. Most labs buy a card when
a flag was sufficient.}

% -------------------------------------------------------------------------
\begin{frame}{Cost per million tokens: self-hosting vs.\ API}
  \begin{equation*}
    \text{\$ per 1M tokens}
      \;=\;
      \frac{\text{GPU \$/hour}}{\text{tokens/sec} \times 3600}
      \times 10^{6}
  \end{equation*}
  \vspace{0.4em}
  \begin{itemize}
    \item \warnc{tokens/sec must be your \emph{aggregate} throughput}, across
          all concurrent requests. So batching is an \emph{economic} decision.
    \item An idle GPU that you own costs the same as a busy one.
  \end{itemize}
  \vspace{0.4em}
  \begin{center}
    \begin{minipage}{0.9\textwidth}
      \begin{block}{When self-hosting wins}
        \small
        An API beats self-hosting unless you have
        \textbf{sustained high utilization}, a \textbf{fine-tuned model}, or
        \textbf{data that legally cannot leave the building}.

        \vspace{0.2em}
        Taiwanese manufacturing clients often have the last one. That is
        why on-prem serving is a job here.
      \end{block}
    \end{minipage}
  \end{center}
\end{frame}
\note{\textbf{33:00.} Notebook Part 5 runs this for any \$/hour that you name.
Give them the sentence that they can say to a PI: ``at our utilization this
costs X per million tokens, and the API costs Y''.}


% -------------------------------------------------------------------------
\begin{frame}{Benchmarking: five rules for reproducible measurements}
  \begin{enumerate}
    \item \textbf{Warm up first.} The first request pays for CUDA graph
          capture, allocator growth and JIT.
    \item \textbf{Fix the concurrency} and state it. A number without a batch
          size means nothing.
    \item \textbf{Use realistic input \emph{and} output lengths.}
          A 20-token prompt tells you nothing about a RAG deployment.
    \item \textbf{Report TTFT, ITL and throughput together.}
          One of them alone misleads.
    \item \textbf{Re-run the arithmetic} from the VRAM and bandwidth formulas
          against what the server reports. The two must agree. If they do not,
          suspect the benchmark first.
  \end{enumerate}
\end{frame}
\note{\textbf{34:00.} Point 5 is the whole talk in one instruction: the
arithmetic is a check on your measurement, and the measurement is a check on
your arithmetic.}

% -------------------------------------------------------------------------
\begin{frame}{Deployment sizing is arithmetic on two numbers}
  \vspace{1.4em}
  \begin{center}
    \begin{minipage}{0.9\textwidth}
      \begin{alertblock}{}
        \centering\large
        The model you can serve is more useful than the model you cannot,
        and memory bandwidth and VRAM decide \emph{which one you can serve}.

        \vspace{0.5em}
        \textbf{You can compute both in advance.}
      \end{alertblock}
    \end{minipage}
  \end{center}
\end{frame}
\note{\textbf{37:00.} Say it once and stop.}

% -------------------------------------------------------------------------
\begin{frame}[fragile]{Questions}
  \vspace{0.6em}
  \begin{center}
    {\Large\bfseries Questions}
  \end{center}
  \vspace{1.2em}
  \begin{center}\small
    Notebook and slides:\\[0.3em]
    \url{https://github.com/Chung-I/MiRA_training_course_2026}\\[0.8em]
    Open the serving demo directly in Colab:\\[0.3em]
    {\scriptsize\url{https://colab.research.google.com/github/Chung-I/MiRA_training_course_2026/blob/main/notebooks/llm_serving_demo.ipynb}}
  \end{center}
  \vspace{0.8em}
  \begin{center}\footnotesize
    Parts 1--5 run on a free CPU runtime.
  \end{center}
\end{frame}
\note{\textbf{37:30 to 40:00 for questions.} If the room is quiet, leave the checklist slide up
instead of this one. Backup slides follow.}

% =========================================================================
\appendix
% =========================================================================

\begin{frame}[standout]
  Appendix\\[0.4em]
  {\normalsize Cut candidates, in cut order}
\end{frame}
\note{Show these only if someone asks. Cut order comes from the plan: Movement 3d
first, then the tail of the toolbox. Keep Movements 3b and 3c, because
the named engines and the robot contrast are what this room will use.}

% -------------------------------------------------------------------------
\begin{frame}{Backup: vLLM and SGLang pip install issues}
  \small
  Both install from \texttt{pip}. On an RTX 5090 with no CUDA toolkit,
  \emphc{neither served from that install}. Both ran in their official
  containers.
  \vspace{0.5em}
  \begin{center}
    \begin{tabular}{lll}
      \toprule
      & \textbf{what broke} & \textbf{why} \\
      \midrule
      vLLM    & \texttt{nvcc} not found        & DeepGEMM wants a toolkit \\
              & \texttt{ninja} not found       & venv \texttt{bin} not on \texttt{PATH} \\
              & compiler/header mismatch       & nvcc 13.3 vs CUDA 13.0 headers \\[0.4em]
      SGLang  & FlashInfer JIT fails           & no \texttt{nvcc}, and the wheel ships only \texttt{ptxas} \\[0.4em]
      \bottomrule
    \end{tabular}
  \end{center}
  \vspace{0.6em}
  Both engines wanted a compiler for \textbf{FP8 and JIT paths that this fp16
  benchmark never executes}.
  \vspace{0.3em}
  \begin{center}
    \emphc{Run them in their official containers.}
  \end{center}
\end{frame}
\note{Backup only. If someone asks "is it really just pip install", use this. The
venv-bin-on-PATH one is the general trap: it looks like a missing
dependency, but it is a PATH problem.}

note{Backup. The negative result, and the answer to ``why is TRT-LLM last in
your table'': we benchmarked it on the wrong workload and kept it to make
that point.}

% -------------------------------------------------------------------------
\begin{frame}{Backup: AWQ, GPTQ, NVFP4 quantization mechanisms}
  \llmdiagW
  \vspace{-0.2em}
  \small
  \begin{itemize}\setlength{\itemsep}{0.6em}
    \item \textbf{GPTQ} (Frantar et al., ICLR 2023) quantizes weights one
          column at a time using an approximate second-order method. Each
          column is rounded, and the rounding error is compensated by
          adjusting all remaining columns via the inverse Hessian. A
          calibration set of $\sim$128 examples computes the Hessian.
          GPTQ runs in minutes on one GPU.
    \item \textbf{AWQ} (Lin et al., MLSys 2024) observes that $\sim$1\% of
          weights process large-magnitude activations and matter
          disproportionately. AWQ applies per-channel scaling before
          quantization: it multiplies important weight channels by a scale
          factor and divides the activations by the same factor, shifting
          the quantization grid to protect those weights.
    \item \textbf{NVFP4} stores each weight in 4 bits of floating-point
          (1 sign, 2 exponent, 1 mantissa). A block of 16 values shares
          an E4M3 scale factor, and the tensor has a global FP32 scale.
          Requires Blackwell fifth-generation Tensor Cores.
  \end{itemize}
\end{frame}
\note{This slide is for the audience member who asks ``what does AWQ actually
do?'' The key difference: GPTQ compensates rounding error through the Hessian.
AWQ protects salient weights by rescaling before quantization. NVFP4 uses a
hardware-native 4-bit float format with fine-grained block scaling.

arXiv:2210.17323 (GPTQ), arXiv:2306.00978 (AWQ).}
% -------------------------------------------------------------------------
\begin{frame}{Backup: NVFP4 stores weights in 4-bit floats on Blackwell}
  \small
  NVFP4 uses 1 sign bit, 2 exponent bits, and 1 mantissa bit. The
  representable range is $-6$ to $+6$.
  \vspace{0.4em}
  \begin{center}
    \begin{tabular}{lrrr}
      \toprule
      \textbf{format} & \textbf{bits} & \textbf{vs FP16} & \textbf{block scaling} \\
      \midrule
      FP8 (E4M3)  & 8 & 2$\times$ smaller  & per-tensor \\
      \emphc{NVFP4} & 4 & \emphc{3.5$\times$ smaller} & two-level: E4M3 per 16
        values, FP32 per tensor \\
      MXFP4       & 4 & 3.5$\times$ smaller & power-of-two per 32 values \\
      \bottomrule
    \end{tabular}
  \end{center}
  \vspace{0.4em}
  \begin{itemize}\small
    \item NVFP4 scales at \textbf{two levels}: each block of 16 values shares
          an E4M3 scale factor, and the whole tensor has an FP32 scale. The
          16-value block and the floating-point scale preserve more dynamic
          range than MXFP4, which uses 32-value blocks with a power-of-two
          scale.
    \item On DeepSeek-R1-0528, NVFP4 lost $\leq$1\% accuracy vs.\ FP8 across
          seven benchmarks. On AIME 2024 it was 2\% \emph{better} than FP8.
    \item Requires \textbf{Blackwell} (fifth-gen Tensor Cores). Not available
          on Hopper or older.
  \end{itemize}
\end{frame}
\note{Backup. MXFP4 uses a coarser block (32 values) with a power-of-two
scale, which loses more precision on outlier-heavy weight distributions.
NVFP4 keeps a floating-point scale on a 16-value block.

Source: NVIDIA developer blog, ``Introducing NVFP4''.}

% -------------------------------------------------------------------------
\begin{frame}{\cutbadge{2: trim next} \; \#6 A smaller model, and \#7 more GPUs, last}
  \begin{columns}[T]
    \begin{column}{0.49\textwidth}
      \textbf{\#6 Serve a smaller model}\\[0.3em]
      \small
      A distilled or smaller model that \emph{passes your eval} beats a
      big one that is too slow to use.\\[0.5em]
      Also: one base model with many \textbf{LoRA adapters} instead of many
      full fine-tunes. The adapters are megabytes. You load the base model
      once.\\[0.4em]
      \emph{(Adapters here are a serving trick. Training is another talk.)}
    \end{column}
    \begin{column}{0.49\textwidth}
      \textbf{\#7 More GPUs, last}\\[0.3em]
      \small
      \textbf{Tensor parallel} inside a node. It needs a fast interconnect
      (NVLink), or the bottleneck moves to the wire.\\[0.5em]
      \textbf{Pipeline parallel} across nodes.\\[0.5em]
      \textbf{CPU offload} as a last resort. It is slow for the same
      reason that decode is slow: PCIe bandwidth is more than 10$\times$
      less than HBM bandwidth, and decode is bandwidth-bound.
    \end{column}
  \end{columns}
\end{frame}
\note{If you only have one sentence: hardware is the most expensive fix and
the least likely to be necessary.}

% -------------------------------------------------------------------------
\begin{frame}{Backup: common questions}
  \small
  \begin{itemize}
    \item \textbf{Self-host or API?} API, unless sustained high volume, a
          fine-tuned model, or data that cannot leave the building. Do the
          \$/1M math.
    \item \textbf{Does quantization hurt quality?} Sometimes, and not
          uniformly across tasks. 4-bit weight-only is good on most tasks, but make sure
          that it passes \emph{your} eval, not perplexity.
    \item \textbf{Why is my model slow on a big GPU?} You are bandwidth-bound
          at batch 1. Batch it, or shrink the bytes.
    \item \textbf{vLLM or SGLang?} vLLM by default. SGLang for structured
          output and prefix reuse.
    \item \textbf{How much VRAM for a 70B?} int4 $\approx$ 35\,GB of weights,
          plus cache. In practice two 40\,GB cards or one 80\,GB card, and
          you will still want a quantized KV cache.
    \item \textbf{CPU offload?} You can, and it will be slow. PCIe is $>10\times$
          slower than HBM.
    \item \textbf{MoE?} Fewer \emph{active} parameters per token, so faster
          decode, but you still hold \emph{all} the experts in VRAM. Good
          for speed, no help for capacity.
  \end{itemize}
\end{frame}
\note{Do not answer these on the main slides. Wait for the questions.}

% -------------------------------------------------------------------------
\begin{frame}{Sources}
  \small
  \begin{itemize}
    \item \textbf{Llama-3-8B} 32 layers / 8 KV heads / head\_dim 128;
          \textbf{Llama-2-7B} 32 KV heads. From the
          \texttt{config.json} of each model. Notebook Part 2 pulls them live.
    \item \textbf{Capacity table} (7 / 18 / 36 users): notebook Part 3,
          24\,GB card, 8k context, 2\,GB overhead.
    \item \textbf{0.920 vs 0.928\,GB, 169.9 vs 1811.6 tok/s, batch 1
          vs 16}: we measured these on an RTX 5090 with Qwen2.5-0.5B-Instruct,
          notebook Parts 6--8.
    \item \textbf{20.4--38.2\% KV utilization, 2--4$\times$ throughput,
          1.7--2.7$\times$ Orca, 22$\times$ FasterTransformer}: Kwon et al.,
          arXiv:2309.06180.
    \item \textbf{17.6 / 44.0 / 21.2 / 13.1\,ms}: figures reported by FlashRT.
          We did not reproduce them.
  \end{itemize}
  \vspace{0.5em}
  \begin{center}\footnotesize
    Bandwidth figures are spec-sheet values (notebook Part 4).
    Prefer a number that you measure on the card that your lab owns.
  \end{center}
\end{frame}
\note{Keep this open during questions. Someone will test the KV
arithmetic, and that is good.}


\end{document}
